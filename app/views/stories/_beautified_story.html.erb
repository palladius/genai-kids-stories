<h2><%= StoryParagraph.emoji %><%= @story.title %> 👷‍♀️ Beauty (v3)</h2>

[This needs a mutation in story DB Schema]

<h3>Obsolete StoryRebuild - Beautified(<%= @story_paragraphs.first.flag rescue "💔" %>)</h3>

<%= render 'stories/obsolete_pointer' %>


<div class='beautified_story' >
  <% @story_paragraphs.each_with_index do |p, ix|
    left_style = "float: left; margin: 0px 15px 15px 0px;"
    right_style = "float: right; margin: 0px 15px 15px 0px;"
    alternative_style = "float: #{ix %2 == 0 ? :left : :right}; margin: 0px 15px 15px 0px;"
  %>
    <div class='beautified_paragraph wrapper'  >
      <p  class="beautified-paragraph demo-2 subhead" >
        <%= render_image_if_attached(p.p_image1, verbose: false , style: alternative_style, width: 200) %>
        <%= raw( p.translated_text.gsub(/\*\*(A.* [1-5])\*\*/) { "<span class='act15' ><b><u>#{Regexp.last_match(1)}</u></b></span><br/><br/>" }.gsub('**','') )%>
      <%#= p.original_text %>
        <%= p.flag %>
        </p>
        <br style="clear:both" />

    </div>
  <% end if @story_paragraphs %>
</div>
