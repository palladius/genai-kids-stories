<p style="color: green"><%= notice %></p>

<%= render @story %>

<!-- BootStrap is really awesome: https://getbootstrap.com/docs/5.3/components/button-group/ -->
<div class="btn-group" role="group" aria-label="Basic example">
  <div class="btn-group-sm me-2" role="group" aria-label="First group">
    <%= link_to "🔙 Back", stories_path, class: "btn btn-outline-success"  %>
    <%= link_to "📝 Edit", edit_story_path(@story),  class: "btn btn-outline-primary" %>
    <%= button_to "💣 Destroy! 💣", @story, method: :delete,  class: "btn btn-danger" %>
  </div>
  <div class="btn-group-sm me-2" role="group" aria-label="Second group">
    <button type="button" class="btn btn-secondary">🚧 New alternative story</button>
    <button type="button" class="btn btn-secondary">🚧 New Sequel</button>
  </div>
  <%#= button_to "🚧 New alternative story", @story, method: :genai_alternative %>
  <%#= button_to "🚧 New Sequel", @story, method: :genai_sequel %>
 <%#= link_to "🚧 Translate to Italian", "/story_rebuilt/#{@story.id}?translate_to=it", method: :post %>
 <%#= button_to "🚧 Translate to Japanese", @story, method: :genai_sequel %>
</div>

<hr/>
<%= render 'stories/story-old', locals: {story: @story } unless prod? %>
